# 📘 Руководство по внедрению i18n в редактор цитат

## 🎯 Цель
Полное внедрение системы интернационализации (i18n) в редактор цитат с поддержкой русского и английского языков.

---

## 📋 Содержание
1. [Подготовка](#подготовка)
2. [Шаг 1: HTML элементы](#шаг-1-html-элементы)
3. [Шаг 2: JavaScript код](#шаг-2-javascript-код)
4. [Шаг 3: Ключи перевода](#шаг-3-ключи-перевода)
5. [Шаг 4: Тестирование](#шаг-4-тестирование)
6. [Чек-лист](#чек-лист)

---

## Подготовка

### Необходимые файлы (уже существуют)
✅ `/js/i18n.js` - основной модуль
✅ `/translate/ru.json` - русские переводы
✅ `/translate/en.json` - английские переводы
✅ `/includes/language_switcher.tpl` - переключатель языка

### Созданные справочные файлы
📄 `i18n_implementation_examples.html` - примеры HTML
📄 `i18n_implementation_javascript.js` - примеры JavaScript
📄 `i18n_translation_keys_list.json` - список всех ключей

---

## Шаг 1: HTML элементы

### 1.1 Добавить переключатель языка

В начале файла (после открывающего `<div class="main-ctr">`):

```html
<div class="main-ctr">
    {include file="includes/language_switcher.tpl"}

    <!-- Остальной код -->
</div>
```

### 1.2 Обновить заголовки

Найти и заменить:

```html
<!-- БЫЛО: -->
<h1>🎨 Универсальный редактор цитат</h1>
<p>Создавайте красивые цитаты с изображением и полным контролем над оформлением</p>

<!-- СТАЛО: -->
<h1 data-i18n="app_title">🎨 Универсальный редактор цитат</h1>
<p data-i18n="app_subtitle">Создавайте красивые цитаты с изображением и полным контролем над оформлением</p>
```

### 1.3 Обновить табы API

```html
<!-- БЫЛО: -->
<button id="tabMovies" class="api-tab active">🎬 Кино</button>

<!-- СТАЛО: -->
<button id="tabMovies" class="api-tab active" data-i18n="tabs.movies">🎬 Кино</button>
```

Повторить для всех табов: `tabMusic`, `tabPexels`.

### 1.4 Обновить placeholders

```html
<!-- БЫЛО: -->
<input type="text" id="apiSearchQuery" placeholder="Введите название фильма...">

<!-- СТАЛО: -->
<input type="text"
       id="apiSearchQuery"
       data-i18n-placeholder="search.placeholder_movie"
       placeholder="Введите название фильма...">
```

### 1.5 Обновить кнопки

Для ВСЕХ кнопок добавить `data-i18n`:

```html
<!-- Примеры: -->
<button class="btn" id="randomQuoteBtn" data-i18n="buttons.generate_ai">🤖 Сгенерировать(ИИ)</button>
<button class="btn" id="downloadBtn" data-i18n="buttons.download">💿 Скачать</button>
<button class="btn" id="apiSearchBtn" data-i18n="buttons.search">🔍 Найти</button>
```

### 1.6 Обновить select options

```html
<!-- БЫЛО: -->
<select id="quoteCategory">
    <option value="random">🎲 Случайная</option>
    <option value="мотивационную">💪 Мотивационная</option>
</select>

<!-- СТАЛО: -->
<select id="quoteCategory">
    <option value="random" data-i18n="categories.random">🎲 Случайная</option>
    <option value="мотивационную" data-i18n="categories.motivational">💪 Мотивационная</option>
</select>
```

### 1.7 Обновить чекбоксы

```html
<!-- БЫЛО: -->
<label class="chk-label">
    <input type="checkbox" id="inclDate"> Включить
</label>

<!-- СТАЛО: -->
<label class="chk-label">
    <input type="checkbox" id="inclDate">
    <span data-i18n="fields.include">Включить</span>
</label>
```

### 1.8 Обновить популярные теги Pexels

```html
<!-- БЫЛО: -->
<button class="pexels-tag" data-query="nature">🌿 Природа</button>

<!-- СТАЛО: -->
<button class="pexels-tag" data-query="nature" data-i18n="tags.nature">🌿 Природа</button>
```

Повторить для всех 20 тегов.

### 1.9 Обновить платформы

```html
<!-- БЫЛО: -->
<span class="platform-name">Instagram Пост</span>

<!-- СТАЛО: -->
<span class="platform-name" data-i18n="platforms.instagram_post">Instagram Пост</span>
```

### 1.10 Обновить подсказки

```html
<!-- БЫЛО: -->
<p style="color: #666;">
    💡 Выберите источник и найдите изображение для фона цитаты.
</p>

<!-- СТАЛО: -->
<p style="color: #666;" data-i18n="hints.select_source">
    💡 Выберите источник и найдите изображение для фона цитаты.
</p>
```

---

## Шаг 2: JavaScript код

### 2.1 Обновить функции уведомлений

Найти функции `QE.showErr` и `QE.showSuccess` (около строки 380):

```javascript
// ЗАМЕНИТЬ:
QE.showErr = function(msg) {
    const errDiv = QE.get('errMsg');
    if (msg) {
        errDiv.textContent = msg;
        errDiv.classList.add('show');
        setTimeout(() => errDiv.classList.remove('show'), 5000);
    }
};

QE.showSuccess = function(msg) {
    const successDiv = QE.get('successMsg');
    if (msg) {
        successDiv.textContent = msg;
        successDiv.classList.add('show');
        setTimeout(() => successDiv.classList.remove('show'), 3000);
    }
};

// НА:
QE.showErr = function(msgKeyOrText) {
    const errDiv = QE.get('errMsg');
    if (msgKeyOrText) {
        const message = window.i18n ? i18n.t(msgKeyOrText, msgKeyOrText) : msgKeyOrText;
        errDiv.textContent = message;
        errDiv.classList.add('show');
        setTimeout(() => errDiv.classList.remove('show'), 5000);
    } else {
        errDiv.classList.remove('show');
    }
};

QE.showSuccess = function(msgKeyOrText) {
    const successDiv = QE.get('successMsg');
    if (msgKeyOrText) {
        const message = window.i18n ? i18n.t(msgKeyOrText, msgKeyOrText) : msgKeyOrText;
        successDiv.textContent = message;
        successDiv.classList.add('show');
        setTimeout(() => successDiv.classList.remove('show'), 3000);
    } else {
        successDiv.classList.remove('show');
    }
};
```

### 2.2 Обновить сообщения об ошибках

Найти и заменить все вызовы:

```javascript
// НАЙТИ:
QE.showErr('Введите текст цитаты')
// ЗАМЕНИТЬ НА:
QE.showErr('messages.error_no_quote')

// НАЙТИ:
QE.showErr('Заполните поле "Автор цитаты"')
// ЗАМЕНИТЬ НА:
QE.showErr('messages.error_no_author')

// НАЙТИ:
QE.showErr('Нет фонового изображения')
// ЗАМЕНИТЬ НА:
QE.showErr('messages.error_no_image')

// И так далее для всех сообщений об ошибках
```

### 2.3 Обновить сообщения об успехе

```javascript
// НАЙТИ:
QE.showSuccess('✅ Изображение скачано!')
// ЗАМЕНИТЬ НА:
QE.showSuccess('messages.success_downloaded')

// НАЙТИ:
QE.showSuccess('✅ Обрезка применена успешно')
// ЗАМЕНИТЬ НА:
QE.showSuccess('messages.success_crop_applied')

// НАЙТИ:
QE.showSuccess('✅ Стикер добавлен!')
// ЗАМЕНИТЬ НА:
QE.showSuccess('messages.success_sticker_added')

// И так далее
```

### 2.4 Обновить динамические статусы

В функции `QE.searchPexels` (около строки 1500):

```javascript
// ЗАМЕНИТЬ:
QE.showSuccess('🔍 Ищу фото на Pexels...');

// НА:
QE.showSuccess('messages.info_searching_pexels');
```

В функции `QE.generateRandomQuote` (около строки 2100):

```javascript
// ЗАМЕНИТЬ:
btn.textContent = '🤖 Генерирую...';
QE.showSuccess('🤖 Генерирую цитату через ИИ...');

// НА:
btn.textContent = i18n.t('buttons.generating');
QE.showSuccess('messages.info_generating_ai');
```

### 2.5 Обновить текст кнопок "Загрузка..."

Найти все места где меняется текст кнопок:

```javascript
// ЗАМЕНИТЬ:
btn.textContent = '⏳ Загружаю...';

// НА:
btn.textContent = i18n.t('buttons.loading');
```

### 2.6 Обновить результаты поиска

В функции `QE.displayPexelsResults`:

```javascript
// ЗАМЕНИТЬ:
resultsContainer.innerHTML = '<p>Ничего не найдено.</p>';

// НА:
const noResults = i18n.t('search.no_results');
resultsContainer.innerHTML = `<p>${noResults}</p>`;
```

### 2.7 Добавить обработчик смены языка

В конце блока `document.addEventListener('DOMContentLoaded', ...)`:

```javascript
document.addEventListener('DOMContentLoaded', function() {
    // ... существующий код ...

    // ДОБАВИТЬ В КОНЕЦ:
    document.addEventListener('languageChanged', function(e) {
        console.log('Язык изменен на:', e.detail.language);

        // Перегенерировать холст
        if (typeof QE !== 'undefined' && QE.genImg) {
            QE.genImg();
        }

        // Обновить текст кнопок, если они были изменены динамически
        const randomBtn = QE.get('randomQuoteBtn');
        if (randomBtn && !randomBtn.disabled) {
            randomBtn.textContent = i18n.t('buttons.generate_ai');
        }

        const jsonBtn = QE.get('jsonQuoteBtn');
        if (jsonBtn) {
            jsonBtn.textContent = i18n.t('buttons.generate_random');
        }

        const downloadBtn = QE.get('downloadBtn');
        if (downloadBtn) {
            downloadBtn.textContent = i18n.t('buttons.download');
        }
    });
});
```

---

## Шаг 3: Ключи перевода

### 3.1 Скопировать ключи в ru.json

Открыть файл `/home/user/Cytat/translate/ru.json` и добавить все ключи из файла `i18n_translation_keys_list.json`.

**Текущая структура:**
```json
{
  "app_title": "🎨 Универсальный редактор цитат",
  "app_subtitle": "Создавайте красивые цитаты...",
  "tabs": {
    "movies": "🎬 Кино",
    "music": "🎵 Музыка"
  }
  // ... добавить остальные ключи
}
```

### 3.2 Создать английский перевод в en.json

Открыть файл `/home/user/Cytat/translate/en.json` и создать английские версии:

```json
{
  "app_title": "🎨 Universal Quote Editor",
  "app_subtitle": "Create beautiful quotes with images and full design control",
  "tabs": {
    "movies": "🎬 Movies",
    "music": "🎵 Music",
    "photos": "🖼️ Photos"
  },
  "buttons": {
    "search": "🔍 Search",
    "generate_ai": "🤖 Generate (AI)",
    "generate_random": "📚 Random",
    "download": "💿 Download"
  }
  // ... добавить остальные ключи с английским переводом
}
```

---

## Шаг 4: Тестирование

### 4.1 Базовая проверка

Открыть страницу в браузере и в консоли выполнить:

```javascript
// 1. Проверка инициализации
typeof i18n !== 'undefined'  // Должно быть true

// 2. Проверка текущего языка
i18n.getCurrentLanguage()    // 'ru' или 'en'

// 3. Тест перевода
i18n.t('app_title')          // Должен вернуть "🎨 Универсальный редактор цитат"

// 4. Тест несуществующего ключа
i18n.t('missing.key')        // Должен вернуть 'missing.key'

// 5. Смена языка
i18n.switchLanguage('en')    // UI должен обновиться

// 6. Проверка количества переведенных элементов
document.querySelectorAll('[data-i18n]').length  // Должно быть >100
```

### 4.2 Функциональные тесты

✅ **Кнопки:**
- Нажать на кнопки генерации - текст должен меняться на "Генерирую..." и обратно
- Проверить что текст переведен

✅ **Уведомления:**
```javascript
QE.showErr('messages.error_no_quote')    // Должна показать переведенную ошибку
QE.showSuccess('messages.success_downloaded')  // Должна показать переведенный успех
```

✅ **Поиск:**
- Открыть вкладку "Кино" - placeholder должен быть "Введите название фильма..."
- Открыть вкладку "Музыка" - placeholder должен смениться на "Введите имя исполнителя..."
- Переключить язык - placeholders должны обновиться

✅ **Динамический контент:**
- Выполнить поиск Pexels
- Проверить что "Ничего не найдено" переведено
- Проверить что "Загрузка..." переведено

✅ **Смена языка:**
- Переключить на английский
- Проверить что ВСЕ элементы обновились
- Обновить страницу - язык должен сохраниться

---

## Чек-лист

### HTML элементы

- [ ] Добавлен переключатель языка
- [ ] Обновлены заголовки (h1, h3)
- [ ] Обновлены табы (Movies, Music, Photos)
- [ ] Обновлены все кнопки (~30 штук)
- [ ] Обновлены placeholders (10+ полей)
- [ ] Обновлены select options (категории, размеры, платформы)
- [ ] Обновлены чекбоксы
- [ ] Обновлены теги Pexels (20 штук)
- [ ] Обновлены платформы (9 штук)
- [ ] Обновлены подсказки и hints

### JavaScript

- [ ] Обновлены функции QE.showErr и QE.showSuccess
- [ ] Заменены все сообщения об ошибках
- [ ] Заменены все сообщения об успехе
- [ ] Обновлены статусы загрузки
- [ ] Обновлен текст кнопок при динамическом изменении
- [ ] Обновлены результаты поиска
- [ ] Добавлен обработчик languageChanged

### Ключи перевода

- [ ] Добавлены все ключи в ru.json
- [ ] Создан полный перевод в en.json
- [ ] Проверена структура JSON (нет синтаксических ошибок)

### Тестирование

- [ ] Проверена инициализация i18n
- [ ] Проверена смена языка через UI
- [ ] Проверены все кнопки
- [ ] Проверены все уведомления
- [ ] Проверен динамический контент
- [ ] Проверено сохранение языка в localStorage
- [ ] Проверена работа на мобильных устройствах

---

## 🎉 Завершение

После выполнения всех шагов:

1. Коммит изменений в Git
2. Тестирование на production окружении
3. Мониторинг отзывов пользователей

### Полезные команды для поиска и замены

```bash
# Найти все незакрытые строки с текстом (возможно пропущенные переводы)
grep -r "Введите\|Загрузка\|Ошибка\|Успешно" *.html *.tpl

# Найти все QE.showErr без ключей
grep "QE.showErr(" *.html *.tpl | grep -v "messages."

# Найти все кнопки без data-i18n
grep "<button" *.html *.tpl | grep -v "data-i18n"
```

---

## 📚 Дополнительные ресурсы

- **Файлы с примерами:**
  - `i18n_implementation_examples.html` - примеры HTML
  - `i18n_implementation_javascript.js` - примеры JavaScript
  - `i18n_translation_keys_list.json` - все ключи

- **Документация i18n:**
  - `/docs/i18n_guide.md` - полное руководство
  - `/integration_example.html` - рабочий пример

- **Шпаргалка:**
  ```
  data-i18n="key"              - перевести текст
  data-i18n-placeholder="key"  - перевести placeholder
  data-i18n-title="key"        - перевести title
  i18n.t('key')                - получить перевод в JS
  i18n.switchLanguage('ru')    - сменить язык
  i18n.applyTranslations()     - применить переводы
  ```

---

## ❓ Часто задаваемые вопросы

**Q: Ключ не переводится, показывается сам ключ**
A: Проверьте что ключ добавлен в оба файла: ru.json и en.json

**Q: После динамического создания элементы не переводятся**
A: Вызовите `i18n.applyTranslations()` после добавления HTML

**Q: Placeholder не обновляется**
A: Используйте `data-i18n-placeholder`, а не `data-i18n`

**Q: Как добавить переменные в перевод?**
A: Используйте `.replace()`:
```javascript
const msg = i18n.t('messages.success_image_loaded').replace('{size}', sizeKB);
```

**Q: Язык не сохраняется после перезагрузки**
A: Проверьте localStorage в браузере, возможно блокируется настройками приватности

---

**Удачи с внедрением! 🚀**
